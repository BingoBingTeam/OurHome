<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/app_main</item>
        <item name="colorPrimaryDark">@color/app_main_dark</item>
        <item name="colorAccent">@color/app_text</item>
    </style>
    <style name="IndexStyle" parent="AppTheme">
        <!--<item name="android:windowDisablePreview">true</item>-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:textAllCaps">false</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@mipmap/img_welcome</item>
    </style>

    <style name="tab_appearance">
        <item name="android:textSize">16sp</item>
    </style>
    <style name="simple_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/Transparency</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
